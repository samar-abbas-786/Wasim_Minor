<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <link rel="stylesheet" href="../stylesheets/profile2.css" />
  </head>
  <body>
    <div id="navbar">
      <div id="logo">
        <img src="../images/pngwing.com (1).png" alt="Logo" />
      </div>
      <div id="nav-item">
        <li><a href="/" id="home">HOME</a></li>
        <li>
          <a href="/profile/<%= encodeURIComponent(user.email) %>" id="services"
            >PROFILE</a
          >
        </li>
        <li><a href="/#aboutUs" id="about">ABOUT</a></li>
        <li><a href="/#contactUs" id="contact">CONTACT</a></li>
        <li><a href="/login" id="login">LOGIN</a></li>
      </div>
      <div id="help1">
        <i id="mode" class="ri-sun-fill"></i>
        <a href="tel:+91 9523366855" id="help"
          >HelpLine
          <i class="fa-solid fa-phone fa-fade"></i>
        </a>
      </div>
    </div>
    <div class="container">
      <h2>User Profile</h2>
      <div class="header">
        <a href="/logout" class="logout-link">Log Out</a>
      </div>
      <div class="profile-info">
        <% if (user.photoLink) { %>
        <img
          src="<%= user.photoLink %>"
          alt="User Photo"
          class="profile-photo"
        />
        <% } %>

        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Age:</strong> <%= user.age %></p>
        <p><strong>Mobile:</strong> <%= user.mobile %></p>
        <p><strong>Gender:</strong> <%= user.gender %></p>
        <p><strong>Address:</strong> <%= user.address %></p>
        <p>
          <strong>Photo Link:</strong>
          <a href="<%= user.photoLink %>" target="_blank">View Photo</a>
        </p>
        <p>
          <strong>Your's Appointment: <a href="">All Appointment</a></strong>
        </p>

        <!-- Display user role based on isDoctor flag -->
        <p>
          <strong>Role:</strong> <%= user.isDoctor ? "Doctor" : "Patient/User"
          %>
        </p>
      </div>

      <form
        action="/profile/<%= encodeURIComponent(user.username) %>"
        method="POST"
        class="update-form"
      >
        <h3>Update Profile</h3>
        <input
          type="text"
          name="username"
          placeholder="Username"
          value="<%= user.username %>"
          required
        />
        <input
          type="email"
          name="email"
          placeholder="Email"
          value="<%= user.email %>"
          required
        />
        <input
          type="number"
          name="age"
          placeholder="Age"
          value="<%= user.age %>"
          required
        />
        <input
          type="text"
          name="mobile"
          placeholder="Mobile Number"
          value="<%= user.mobile %>"
          required
        />
        <input
          type="text"
          name="gender"
          placeholder="Gender"
          value="<%= user.gender %>"
          required
        />
        <input
          type="text"
          name="address"
          placeholder="Address"
          value="<%= user.address %>"
          required
        />
        <input
          type="text"
          name="photoLink"
          placeholder="Photo Link"
          value="<%= user.photoLink %>"
        />
        <button type="submit">Update</button>
      </form>

      <a href="/apply-doctor" class="apply-doctor">Apply to be a Doctor</a>
    </div>
  </body>
</html>
